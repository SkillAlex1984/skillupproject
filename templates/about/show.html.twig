{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }}/о товарах
{% endblock %}
{% block body %}
About
<a href="{{ notebooksUrl }}">dvd</a> <br>
<a href="{{ path('category_show', {'slug': 'tvs', 'page':5}) }}">TVs</a> <br>
<a href="{{ url('category_show', {'slug': 'music'}) }}">Music</a>  <br>

Last visited category: {{ lastCategory }}

{% endblock %}