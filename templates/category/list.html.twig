{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} - список категорий товаров
{% endblock %}
{% block body %}
        {% for category in categories %}
            <a href="{{ path('category_show', {'slug': category.slug}) }}">{{ category.name }}</a>
        <hr>
        {% endfor %}
{% endblock %}