{% extends 'base.html.twig' %}

{% block body %}

    <h1>Корзина</h1>
        <div class="container">
            {% include 'orders/items.html.twig' with {'order': order} %}

            <a href="{{ path('order_complete') }}">Офорить заказ</a>
        </div>
{% endblock %}